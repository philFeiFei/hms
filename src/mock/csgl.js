import Mock from 'mockjs'
import { param2Obj } from '@/utils'
export default {
  addParams: config => {
    var result = {
      _success: true,
      code: null,
      _message: ''
    }
    return result
  }
}
